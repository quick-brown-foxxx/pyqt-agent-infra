# Fix Plan — 2026-04-05

Based on triage from 3 agents (coverage, bugs, docs). Organized into parallel work streams.

## Stream A: Bug Fixes (trivial code changes)

1. `cli.py:110-116` — Delete dead duplicate vagrant ssh-config call
2. `audio.py:43-50` — Use DEVNULL instead of PIPE for pw-loopback Popen
3. `audio.py:170-178` — Verify recording output file exists after pw-record
4. `vm.py:77-83` — Use DEVNULL for vagrant rsync-auto Popen
5. `vm.py:89-95` — Validate display parameter format (regex `:N` or `:N.M`)
6. `tray.py:47-53` — Narrow RuntimeError catch to check for ServiceUnknown
7. `tray.py:49` — Use logger.warning() instead of print()
8. `_server.py:119` — chmod socket to 0o600 after bind
9. `_bootstrap.py:100-101` — Use tempfile.mkstemp with controlled permissions
10. `_protocol.py:16` + `_eval.py:29` — Use Literal type for eval mode
11. `_atspi.py:100` — Remove unnecessary type:ignore

## Stream B: Doc & Config Fixes (text replacements)

1. `skills/qt-dev-tools-setup/SKILL.md` — Fix "not on PyPI" installation instructions
2. `docs/ROADMAP.md` — Fix wrong skill names in Phase 5.1 (2 edits)
3. `docs/ROADMAP.md` — Fix non-existent doc references (4 edits: 4.6, 5.3, 5.6, 6.0g)
4. `CLAUDE.md` — Remove "except 7.7 manual testing"
5. `CLAUDE.md` — Fix "xclip wrapper" → "xsel/xclip wrapper"
6. `README.md` — Fix "clipboard (xclip)" → "clipboard (xsel/xclip)"
7. `DEVELOPMENT.md` — Add missing Makefile targets to table
8. `pyproject.toml` — Add pytest-timeout to dev deps
9. `pyproject.toml` — Add Python 3.13/3.14 classifiers
10. `Makefile` — Fix test-e2e to use uv run pytest
11. `.pre-commit-config.yaml` — Fix basedpyright to run on src/ not per-file

## Stream C: Provision Fix (PulseAudio/PipeWire conflict)

1. Edit `provision.sh.j2` — Remove PulseAudio daemon start from .bashrc
2. Regenerate root `provision.sh` from template
3. Update root `Vagrantfile` if needed

## Stream D: New Tests — E2E (the highest-value work)

1. E2E test for `fill` compound command
2. E2E test for `do --verify` compound command
3. E2E test for `wait --app` command
4. CLI failure mode tests (click no match, screenshot fail)
5. Add markers to test_main.py

## Stream E: Unit Test Fixes

1. Rewrite clipboard tests — cover both xsel and xclip paths
2. Fix interact.py test_focus_falls_back_to_click weak assertion
3. Add notify.listen() integration test with real parsed output
4. Add installer.py unit tests (init_toolkit, self_update)

## Execution Order

Streams A + B + C are independent → parallel.
Stream D depends on provision fix (C) for audio tests.
Stream E is independent of everything.

## Decisions Made

- **Security fixes**: Include socket permissions and display validation despite "low priority" because they're trivial (1-line each)
- **Result pattern**: NOT adopting — too large a refactor for the value. Keep exceptions.
- **cli.py split**: NOT doing now — refactoring without other motivation
- **Tray SNI enablement**: NOT doing — too much infra work for uncertain value
- **notify.py isinstance**: Bug triage confirmed this is NOT a bug (TimeoutExpired.stdout is bytes even with text=True)
